---
name: github-skill-release
description: 安全地将 Codex skill 打包、发布和迭代到 GitHub，包括依赖 skill 融合检查、用户确认仓库和 Release 设置、秘密/隐私/路径审计、GitHub CLI 授权、GitHub Release 发布与最终核验。适用于用户要求开源、发布、更新、迭代或 release 一个 Codex skill 仓库时。
---

# GitHub Skill Release

为 Codex skill 准备一个干净、可审计的 GitHub 仓库，避免泄露本机信息，也避免擅自替用户扩大公开范围或发布设置。

中文本地代称：`GitHub技能开源发布`。标准调用名仍为 `$github-skill-release`。

## 必守边界

- 从目标 skill 的干净发布副本开始工作。不要发布整个 `.codex/skills` 目录、任务历史、缓存、本地运行时目录或无关文件。
- 在创建或修改任何远程仓库设置前，必须让用户从明确选项中选择，并为每个重要设置提供自定义选项。相关设置包括可见范围、许可证、仓库名、描述、README 处理方式、`.gitignore`、Issues、Wiki 和推送时机。
- 公开发布在实践上应视为不可逆：公开仓库内容可能被复制、索引或缓存。
- 发布 skill 仓库的 `README.md` 默认使用中文。只有当用户明确选择其他语言或双语 README 时，才按用户选择执行，并在发布总结中记录 README 语言。
- 不要要求用户在对话中粘贴密码、token、一次性验证码或恢复码。遇到浏览器登录、通行密钥、验证码或二次验证时，暂停并让用户本人完成。
- 如果用户说正在登录，不要轮询浏览器页面状态，也不要打开额外登录标签页，直到用户明确说可以继续。
- 绝不在仓库文件中打印或保存 GitHub token。使用设备授权流程时，只能把临时授权状态保存在发布树之外，并在用完后删除。

## 工作流程

发布 skill 时，读取并遵循 [references/release-workflow.md](references/release-workflow.md) 中的相关章节。

在将要提交和推送的精确目录上运行 `scripts/audit_release_tree.py`。高严重度问题必须先处理，除非用户明确决定不发布对应内容。

在最终确定发布树前运行 `scripts/inspect_skill_dependencies.py`。该脚本用于发现目标 skill 是否显式依赖其他 skill。显式依赖通常表现为说明中直接调用“美元符号加 skill 名”的语法；只在普通文字中提到某类内置能力时，通常属于需要人工判断的可能依赖。若发现真实绑定，应把必要且可公开的说明或资源融合进发布树，让用户安装公开仓库后不需要依赖作者本地的私有 skill。不要在未确认版权或公开权限时逐字复制私有、系统、内置或第三方 skill 内容。

GitHub 操作优先使用已安装且已登录的 GitHub CLI，也就是 `gh`。如果缺少 `gh`，识别当前操作系统，并向用户说明官方安装方式或系统包管理器方案；涉及安装软件、管理员权限或系统修改时，先获得确认。如果 `gh auth login --web` 无法访问 GitHub 授权接口，但 PowerShell HTTPS 请求可用，可按流程参考中的设备授权备用方案处理，且不得把秘密信息暴露到对话或发布树。

发布 GitHub Release 前，必须让用户选择标签/版本号、Release 标题、发布说明、草稿/预发布状态以及是否附加资产。解释版本格式 `vMAJOR.MINOR.PATCH`：`v` 表示版本标签，`MAJOR` 用于大的或破坏性更新，`MINOR` 用于新增优化/功能，`PATCH` 用于不新增功能模块的修复。

更新已发布 skill 时，读取流程参考中的更新章节。每次完成更新推送后，都必须询问用户是否发布 GitHub Release，选项应包含立即发布稳定版、创建草稿、创建预发布版、跳过本次 Release 和自定义。

## 完成标准

- 发布树只包含预期公开文件。
- README 语言符合用户选择；未另选时默认为中文。
- 必要的 skill 依赖已经融合进发布树，或已按用户确认的方式处理。
- 安全审计没有未解决的秘密、个人信息、本地绝对路径、临时文件、测试残留或其他不应公开内容。
- 仓库设置符合用户选择。
- 已推送的 GitHub 仓库可访问，可见范围、功能设置和文件清单符合预期。
- 对于更新任务，最终差异只包含预期 skill 改动，并且更新推送后已询问用户是否发布 GitHub Release。
- 如用户要求发布 Release，Release 的标签、标题、说明、状态和资产符合用户确认。
- 临时核验克隆和临时授权状态文件已清理。
